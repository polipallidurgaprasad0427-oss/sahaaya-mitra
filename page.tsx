'use client'

import { useState } from 'react'
import { Activity, AlertTriangle, Bell, CheckCircle2, ChevronDown, ChevronRight, CloudRain, Clock3, FileText, Flame, Globe2, HeartPulse, Home, Languages, LocateFixed, Map, Menu, MoreHorizontal, Phone, Radio, Search, ShieldCheck, Siren, SlidersHorizontal, Thermometer, Users, X, Zap } from 'lucide-react'
import { languages, useLanguage, type Language } from '@/components/language-context'
const requests = [
  { id: 'RQ-1048', place: 'Kokrajhar, Assam', kind: 'Flooding', people: 4, time: '8 min ago', level: 'critical' },
  { id: 'RQ-1047', place: 'Imphal East, Manipur', kind: 'Landslide', people: 2, time: '22 min ago', level: 'high' },
  { id: 'RQ-1046', place: 'Dimapur, Nagaland', kind: 'Medical', people: 1, time: '41 min ago', level: 'medium' },
]

function Metric({ icon: Icon, label, value, detail, tone = 'default' }: { icon: typeof Activity; label: string; value: string; detail: string; tone?: string }) {
  return <div className="metric-card"><div className={`metric-icon ${tone}`}><Icon size={18} /></div><div><p className="eyebrow">{label}</p><p className="metric-value">{value}</p><p className="metric-detail">{detail}</p></div></div>
}

function MapPanel() {
  return <section className="panel map-panel"><div className="panel-header"><div><p className="eyebrow">REGIONAL OVERVIEW</p><h2>Risk zones</h2></div><button className="small-button"><LocateFixed size={15} />Locate me</button></div><div className="map-visual"><div className="map-grid" /><div className="map-river river-one" /><div className="map-river river-two" /><div className="map-region region-a"><span>ARUNACHAL<br />PRADESH</span></div><div className="map-region region-b"><span>ASSAM</span></div><div className="map-region region-c"><span>MANIPUR</span></div>{[['Kokrajhar', 'critical', '24%', '28%'], ['Guwahati', 'high', '54%', '51%'], ['Imphal', 'medium', '72%', '73%'], ['Dimapur', 'high', '84%', '62%'], ['Itanagar', 'low', '45%', '12%']].map(([label, level, left, top]) => <button key={label} className={`map-pin ${level}`} style={{ left, top }} aria-label={`${label}, ${level} risk`}><span /><strong>{label}</strong></button>)}<div className="map-controls"><button aria-label="Zoom in">+</button><button aria-label="Zoom out">−</button></div><div className="map-legend"><span><i className="dot critical" />Critical</span><span><i className="dot high" />High</span><span><i className="dot medium" />Moderate</span></div></div><div className="map-footer"><span><Globe2 size={15} /> 8 districts monitored</span><button>Open full map <ChevronRight size={14} /></button></div></section>
}

function RequestsPanel({ resolved, setResolved }: { resolved: string[]; setResolved: (ids: string[]) => void }) {
  return <section className="panel requests-panel"><div className="panel-header"><div><p className="eyebrow">ACTION REQUIRED</p><h2>Rescue requests <span className="count-badge">12</span></h2></div><button className="text-button">View all <ChevronRight size={15} /></button></div><div className="request-list">{requests.map(request => <div className={`request-row ${resolved.includes(request.id) ? 'resolved' : ''}`} key={request.id}><div className={`request-symbol ${request.level}`}><HeartPulse size={18} /></div><div className="request-info"><strong>{request.place}</strong><span>{request.kind} · {request.people} {request.people === 1 ? 'person' : 'people'}</span></div><div className="request-meta"><span className={`level-tag ${request.level}`}>{resolved.includes(request.id) ? 'Assigned' : request.level}</span><small>{request.time}</small></div><button className="row-action" onClick={() => setResolved([...resolved, request.id])} aria-label={`Assign ${request.id}`}><ChevronRight size={17} /></button></div>)}</div></section>
}

export default function Page() {
  const { language, setLanguage, t } = useLanguage(); const [activeNav, setActiveNav] = useState('Home'); const [menuOpen, setMenuOpen] = useState(false); const [languageOpen, setLanguageOpen] = useState(false); const [alertOpen, setAlertOpen] = useState(true); const [reportOpen, setReportOpen] = useState(false); const [safe, setSafe] = useState(false); const [resolved, setResolved] = useState<string[]>([])
  const navItems = [{ name: t('nav.home'), key: 'Home', icon: Home }, { name: 'Dashboard', key: 'Dashboard', icon: Activity }, { name: t('nav.alerts'), key: 'Alerts', icon: Bell, count: '3' }, { name: 'Rescue requests', key: 'Rescue requests', icon: Siren, count: '12' }, { name: t('nav.map'), key: 'Map view', icon: Map }, { name: 'Reports', key: 'Reports', icon: FileText }]
  const go = (name: string) => { setActiveNav(name); setMenuOpen(false) }
  const showMap = activeNav === 'Map view'; const showReports = activeNav === 'Reports' || activeNav === 'Rescue requests'; const overview = activeNav === 'Home' || activeNav === 'Dashboard' || activeNav === 'Alerts'
  return <div className="app-shell"><aside className={`sidebar ${menuOpen ? 'open' : ''}`}><div className="brand"><div className="brand-mark"><Radio size={20} /></div><div><strong>SahaayaMitra</strong><span>rescue intelligence</span></div><button className="close-menu" onClick={() => setMenuOpen(false)} aria-label="Close navigation"><X size={18} /></button></div><div className="network-status"><span className="status-dot" />System online <span className="sync-label">Synced 2m ago</span></div><nav aria-label="Primary navigation">{navItems.map(({ name, key, icon: Icon, count }) => <button key={key} onClick={() => go(key)} className={`nav-item ${activeNav === key ? 'active' : ''}`}><Icon size={18} /><span>{name}</span>{count && <b>{count}</b>}{activeNav === key && <ChevronRight className="nav-arrow" size={15} />}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item"><SlidersHorizontal size={18} /><span>Settings</span></button><div className="profile"><div className="avatar">AM</div><div><strong>Ananya Mehta</strong><span>District responder</span></div><MoreHorizontal size={17} /></div></div></aside><main className="main-content"><header className="topbar"><button className="menu-button" onClick={() => setMenuOpen(true)} aria-label="Open navigation"><Menu size={21} /></button><div className="breadcrumb"><span>North Eastern Region</span><ChevronRight size={15} /><strong>{activeNav}</strong></div><div className="top-actions"><div className="language-wrap"><button className="top-button" onClick={() => setLanguageOpen(!languageOpen)}><Languages size={17} />{languages.find(item => item.code === language)?.native ?? 'English'}<ChevronDown size={14} /></button>{languageOpen && <div className="language-menu">{languages.map(item => <button key={item.code} onClick={() => { setLanguage(item.code as Language); setLanguageOpen(false) }}>{item.native} <span>{item.name}</span></button>)}</div>}</div><button className="icon-button" aria-label="Search"><Search size={18} /></button><button className="icon-button notification" aria-label="Notifications"><Bell size={18} /><i /></button><div className="top-avatar">AM</div></div></header><div className="content-wrap"><div className="page-heading"><div><p className="kicker"><span className="live-pulse" />LIVE OPERATIONS CENTER</p><h1>{activeNav === 'Home' ? 'Good morning, Ananya.' : activeNav}</h1><p className="subheading">{activeNav === 'Home' ? "Here's what's happening across the North Eastern Region." : `Review ${activeNav.toLowerCase()} and coordinate the next response.`}</p></div><div className="heading-actions"><button className="outline-button"><Clock3 size={16} />Last 24 hours</button><button className="primary-button" onClick={() => setReportOpen(true)}><Siren size={16} />Report emergency</button></div></div>{overview && <><section className="critical-banner"><div className="banner-icon"><AlertTriangle size={23} /></div><div className="banner-copy"><div className="banner-title"><span className="critical-label">CRITICAL ALERT</span><span className="banner-time">Updated 4 min ago</span></div><h2>Flash flood warning · Brahmaputra basin</h2><p>Water levels rising rapidly near Kokrajhar and Chirang districts. Response teams have been notified.</p></div><button className="banner-action" onClick={() => setAlertOpen(true)}>View alert <ChevronRight size={16} /></button></section><div className="metrics-grid"><Metric icon={AlertTriangle} label="Active alerts" value="03" detail="1 critical · 2 high" tone="red" /><Metric icon={Siren} label="Open rescue requests" value="12" detail="4 need assignment" tone="orange" /><Metric icon={Users} label="People assisted" value="184" detail="+28 in last 24h" tone="green" /><Metric icon={Activity} label="Network coverage" value="87%" detail="5 areas offline" tone="blue" /></div></>}{showMap && <div className="single-section"><MapPanel /></div>}{showReports && <div className="bottom-grid"><RequestsPanel resolved={resolved} setResolved={setResolved} /><section className="panel activity-panel"><div className="panel-header"><div><p className="eyebrow">AUDIT TRAIL</p><h2>Reports & recent activity</h2></div><button className="more-button" aria-label="More options"><MoreHorizontal size={18} /></button></div><div className="activity-list"><div><i className="activity-dot red" /><p><strong>Critical alert issued</strong><span>Brahmaputra basin · 4 min ago</span></p></div><div><i className="activity-dot green" /><p><strong>Rescue request assigned</strong><span>RQ-1045 · 18 min ago</span></p></div><div><i className="activity-dot blue" /><p><strong>Team checked in</strong><span>Guwahati Unit 04 · 31 min ago</span></p></div><div><i className="activity-dot orange" /><p><strong>Weather threshold reached</strong><span>Imphal East · 45 min ago</span></p></div></div><div className="offline-note">5 districts operating in offline mode</div></section></div>}{overview && <div className="dashboard-grid"><MapPanel /><section className="panel weather-panel"><div className="panel-header"><div><p className="eyebrow">FIELD CONDITIONS</p><h2>Regional conditions</h2></div><button className="more-button" aria-label="More options"><MoreHorizontal size={18} /></button></div><div className="weather-main"><div className="weather-icon"><CloudRain size={28} /></div><div><strong>Monsoon activity</strong><span>Heavy rain expected</span></div><div className="weather-temp">28°<small>C</small></div></div><div className="condition-row"><div><span><Thermometer size={15} />Temperature</span><strong>28°C</strong></div><div><span><CloudRain size={15} />Rainfall</span><strong>86 mm</strong></div><div><span><Zap size={15} />Risk index</span><strong className="orange-text">High</strong></div></div><p className="updated">Data from regional weather stations · Updated 6 min ago</p></section></div>}</div></main>{alertOpen && <div className="modal-backdrop"><div className="alert-modal" role="dialog" aria-modal="true" aria-labelledby="alert-title"><button className="modal-close" onClick={() => setAlertOpen(false)} aria-label="Close alert"><X size={18} /></button><div className="modal-alert-icon"><Siren size={26} /></div><p className="critical-label">CRITICAL ALERT · FLOOD</p><h2 id="alert-title">Brahmaputra basin flash flood warning</h2><p className="modal-copy">Water levels are rising rapidly in Kokrajhar and Chirang districts. Residents near the river basin should move to higher ground immediately.</p><div className="alert-facts"><span><Map size={15} /> 2 districts</span><span><Users size={15} /> 18,400 at risk</span><span><Clock3 size={15} /> Issued 4 min ago</span></div><div className="modal-actions"><button className="safe-button" onClick={() => { setSafe(true); setAlertOpen(false) }}><ShieldCheck size={17} />{safe ? 'You are marked safe' : 'I am safe'}</button><button className="trapped-button" onClick={() => { setAlertOpen(false); setReportOpen(true) }}><AlertTriangle size={17} />I need help</button></div><p className="modal-foot"><Phone size={13} /> Emergency services: <strong>112</strong></p></div></div>}{reportOpen && <div className="modal-backdrop"><div className="report-modal" role="dialog" aria-modal="true" aria-labelledby="report-title"><button className="modal-close" onClick={() => setReportOpen(false)} aria-label="Close report"><X size={18} /></button><div className="report-heading"><div className="report-icon"><Siren size={21} /></div><div><p className="eyebrow">URGENT ASSISTANCE</p><h2 id="report-title">Report an emergency</h2></div></div><p className="modal-copy">Your location will be shared with the nearest response team and district administration.</p><div className="emergency-options"><button><CloudRain size={20} />Flood</button><button><Activity size={20} />Landslide</button><button><HeartPulse size={20} />Medical</button><button><Flame size={20} />Fire</button></div><div className="location-field"><LocateFixed size={17} /><div><span>Your location</span><strong>Guwahati, Assam · Accurate</strong></div><CheckCircle2 size={17} /></div><button className="send-report" onClick={() => setReportOpen(false)}><Siren size={17} />Send rescue request</button><button className="cancel-button" onClick={() => setReportOpen(false)}>Cancel</button></div></div>}</div>
}
